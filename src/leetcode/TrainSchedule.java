package leetcode;

public class TrainSchedule {
    public static void main(String[] args) {


//        // create a client
//        var client = HttpClient.newHttpClient();// create a request
//        var request = HttpRequest.newBuilder(
//                        URI.create("https://api-v3.mbta.com/predictions?page%5Blimit%5D=15&sort=departure_time&include=route&filter%5Bstop%5D=place-pktrm"))
//                .header("accept", "application/vnd.api+json")
//                .build();// use the client to send the request
//        var response = client.send(request, new JsonBodyHandler<>(APOD.class));
//        ObjectMapper objmapper = new Object();
//        Dep[] dep = objmapper.readValue(response.body().get(), Dep[].class)// the response:
        // System.out.println(response.body().get().title);
//        string[] line;
//        for (dep d in dep[])
//        if (!line.Contains(d.line))
//            add d.line dep[];
//        for (string a : line[])
//        print what we are looking
//        for (dep b : dep[])
//        if b.line == a
//        print
//        json array to object array - Java
//        Java http calls
    }

}
